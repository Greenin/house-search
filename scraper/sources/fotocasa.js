// Fotocasa tampoco tiene API publica. Se deja como stub excluido del array
// FUENTES en index.js: se implementara mas adelante, despues de validar el
// patron completo (Playwright -> normalize -> insert -> scoring) con
// Idealista en la Fase 3.
export async function buscar() {
  throw new Error('Fotocasa (Playwright) todavia no esta implementado.');
}
